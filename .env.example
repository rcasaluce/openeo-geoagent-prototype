# --- LLM / Gemini (required) ---
TOKEN_GEMINI=your_gemini_api_key_here

# --- Local Knowledge Graph path (optional but recommended) ---
# Path used by openEO_llm.py to load the collections knowledge graph JSON.
OPENEO_COLLECTIONS_KG_PATH=data/collections_kg.json

# --- openEO backend endpoint (optional, if your wrappers/scripts want to read it) ---
OPENEO_ENDPOINT=https://openeo.dataspace.copernicus.eu
